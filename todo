.  String type (with literals and stuff)
-> Operator precedence 
-> User defined types (structs...) {
      .  Struct allocation for scratch and arguments
	  -> AST_OP_DOT `.` notation and field typechecking
	  -> field padding (multiples of 4 Bytes)
   }
.  External functions (call C functions from myc)
.  Misecellaneus types and other stuff
.  implicit integer casts



-> : wip
.  : TODO
